<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
  <ActionBar title="Magic House: Devices" android.icon="res://icon" android.iconVisibility="always"></ActionBar>
  <StackLayout>
    <Button text="Connect" tap="{{ connect }}" class="btn btn-primary scan-button" />
    <Button text="View Home" tap="{{ viewHome }}" class="btn btn-primary scan-button" />
  </StackLayout>
</Page>